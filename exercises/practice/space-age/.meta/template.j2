{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}

{{ macros.header() }}

{% for case in cases -%}
test_that("{{ case["description"] }}", {
  seconds <- {{ case["input"]["seconds"] }}
  expect_equal(space_age(seconds, "{{ case["input"]["planet"] | lower }}"), {{ case["expected"] }})
})

{% endfor %}
