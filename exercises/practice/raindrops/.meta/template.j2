{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}

{{ macros.header() }}

{% for case in cases -%}
test_that("{{ case["description"] }}",{
  number <- {{ case["input"]["number"] }}
  expect_equal(raindrops({{ case["input"]["number"] }}), "{{ case['expected'] }}")
})
{% endfor %}
